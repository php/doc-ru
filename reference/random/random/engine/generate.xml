<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: e2f2172bf114599926878017ab2dce356956fa9e Maintainer: sergey Status: ready -->
<!-- Reviewed: no -->
<refentry xml:id="random-engine.generate" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>Random\Engine::generate</refname>
  <refpurpose>Генерирует случайную последовательность</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis role="Random\\Engine">
   <modifier>public</modifier> <type>string</type><methodname>Random\Engine::generate</methodname>
   <void/>
  </methodsynopsis>
  <para>
   Метод возвращает случайную последовательность и продвигает состояние алгоритма на один шаг.
  </para>
  <para>
   Случайная последовательность возвращается в виде строки, которая состоит из случайных байтов в фиксированном порядке — от младшего к старшему.
   В фиксированном строковом представлении случайные биты интерпретируются однозначно, при этом строка,
   в отличие от других скалярных типов данных, вмещает случайные последовательности произвольной длины, какого бы размера вывод ни сгенерировал конкретный алгоритм.
  </para>
  <para>
   В пользовательских реализациях метода потребуется вернуть двоичное строковое представление числа с порядком байтов от младшего к старшему,
   например путём вызова функции <function>pack</function> с кодом формата <literal>P</literal>,
   если случайная последовательность генерируется через алгоритм, который возвращает целочисленное значение.
   Класс <classname>Random\Randomizer</classname> предоставляет высокоуровневый интерфейс,
   который интерпретирует возвращаемые случайные байты как беззнаковые целочисленные значения с порядком байтов от младшего к старшему,
   когда требуется числовое представление.
  </para>
  <para>
   Из метода стараются возвращать строку, каждый бит которой выбирался равномерно и независимо,
   поскольку отдельным приложениям для корректной работы требуется случайность на уровне битов.
   Линейные конгруэнтные генераторы часто возвращают случайную последовательность меньшего качества
   в младших битах целочисленного значения и поэтому оказываются неприемлемыми для приложений,
   которым требуется случайность на уровне битов.
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  &no.function.parameters;
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   Метод возвращает непустую строку случайных байтов.
  </para>

  <note>
   <simpara>
    Класс <classname>Random\Randomizer</classname> внутренне работает с беззнаковыми 64-разрядными целочисленными значениями,
    поэтому проигнорирует лишние байты, если метод вернёт строку со случайной последовательностью, размер которой превышает 64 бита, или 8 байтов,
    но другие приложения умеют обрабатывать случайные последовательности длиннее 64 битов.
   </simpara>
  </note>
 </refsect1>

 <refsect1 role="errors">
  &reftitle.errors;
  <itemizedlist>
   <listitem>
    <simpara>
     При ошибке генерации случайной последовательности из метода выбрасывают исключение <classname>Random\RandomException</classname>.
     Остальные исключения семейства <classname>Exception</classname>, которые метод выбрасывает при генерации,
     перехватывают и оборачивают исключением <classname>Random\RandomException</classname>.
    </simpara>
   </listitem>
   <listitem>
    <simpara>
     При возврате пустой строки объект <classname>Random\Randomizer</classname>
     выбросит ошибку <classname>Random\BrokenRandomEngineError</classname>.
    </simpara>
   </listitem>
   <listitem>
    <simpara>
     При избыточном статистическом смещении реализованного алгоритма объект <classname>Random\Randomizer</classname>
     выбрасывает ошибку <classname>Random\BrokenRandomEngineError</classname>,
     чтобы предотвратить бесконечный цикл, когда алгоритм выборки с отклонением требует возврата несмещённых результатов.
    </simpara>
   </listitem>
  </itemizedlist>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <example>
   <title>Пример генерации случайной последовательности методом <function>Random\Engine::generate</function></title>
   <programlisting role="php">
<![CDATA[
<?php

/**
 * Класс реализует метод генерации случайных последовательностей
 * через линейный конгруэнтный генератор, который возвращает
 * двоичное строковое представление 8-битного целочисленного значения.
 * Параметры генератора — модуль: 65536, множитель: 61, инкремент: 17.
 *
 * Примечание: Движок реализовали только в целях демонстрации.
 *       Линейные конгруэнтные генераторы часто выдают низкокачественную случайную
 *       последовательность, а конкретная реализация примера ограничивается
 *       недопустимо коротким 16-битным периодом, который нельзя применять
 *       в производственном окружении.
 */
final class LinearCongruentialGenerator implements \Random\Engine
{
    private int $state;

    public function __construct(?int $seed = null)
    {
        if ($seed === null) {
            $seed = random_int(0, 0xffff);
        }

        $this->state = $seed & 0xffff;
    }

    public function generate(): string
    {
        $this->state = (61 * $this->state + 17) & 0xffff;

        return pack('C', $this->state >> 8);
    }
}

$r = new \Random\Randomizer(
    new LinearCongruentialGenerator(seed: 1)
);

echo "Счастливое число: ", $r->getInt(0, 99), "\n";
]]>
   </programlisting>
   &example.outputs;
   <screen>
<![CDATA[
Счастливое число: 4
]]>
   </screen>
  </example>
 </refsect1>

</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
