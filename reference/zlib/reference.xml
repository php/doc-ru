<?xml version="1.0" encoding="windows-1251"?>
<!-- EN-Revision: 1.14 Maintainer: Nick Shaforostoff Status: ready -->
<!-- $Revision: 1.1 $ -->
 <reference id="ref.zlib">
  <title>Функции сжатия Zlib</title>
  <titleabbrev>Zlib</titleabbrev>

  <partintro>
   <section id="zlib.intro">
    &reftitle.intro;
    <para>
     Этот модуль позволяет работать со сжатыми файлами gzip (.gz) стандартным методом, наподобие функций <link linkend="ref.filesystem">файловой системы</link> (исключение составляют только сокеты).
    </para>
    <note>
     <para>
      В версии 4.0.4 появился новый протокол 'zlib:' для доступа прямого к сжатым файлам через обычные функции f*() (для этого нужно было добавить 'zlib:' в начало пути к файлу при вызове
      <function>fopen</function>.
     </para>
     <para>
      В версии 4.3.0, префикс изменился на 'zlib://' для предотвращения многозначности в случае файлов, содержащих ':' в имени.
     </para>
     <para>
      Требуется библиотека времени выполнения C, предоставляющая функцию <literal>fopencookie()</literal>. 
      Насколько мне известно, такой библиотекой являетя только GNU libc.
     </para>
    </note>
   </section>

   <section id="zlib.requirements">
    &reftitle.required;
    <para>
     Этот модуль использует библиотеку <ulink url="&url.zlib;">zlib</ulink>, написанную Jean-loup Gailly и Mark Adler. Используйте версию zlib &gt;= 1.0.9 с этим модулем.
    </para>
   </section>

   &reference.zlib.configure;

   &reference.zlib.ini;

   <section id="zlib.resources">
    &reftitle.resources;
    &no.resource;
   </section>

   &reference.zlib.constants;

   <section id="zlib.examples">
    &reftitle.examples;
    <para>
     Здесь открывается временный файл, в него записывается строка, затем дважды печатается содержимое этого файла.
    </para>
    <example>
     <title>Небольшой пример использования Zlib</title>
     <programlisting role="php">
<![CDATA[
<?php

$filename = tempnam('/tmp', 'zlibtest') . '.gz';
echo "<html>\n<head></head>\n<body>\n<pre>\n";
$s = "Only a test, test, test, test, test, test, test, test!\n";

// открыть файл для записи с максимальным сжатием
$zp = gzopen($filename, "w9");

// записать строку в файл
gzwrite($zp, $s);

// акрыть файл
gzclose($zp);

// открыть файл для чтения
$zp = gzopen($filename, "r");

// прочитать три символа
echo gzread($zp, 3);

// выводить до конца файла, затем закрыть его
gzpassthru($zp);
gzclose($zp);

echo "\n";

// открыть файл и напечатать его содержимое (во второй раз)
if (readgzfile($filename) != strlen($s)) {
        echo "Error with zlib functions!";
}
unlink($filename);
echo "</pre>\n</body>\n</html>\n";

?>
]]>
     </programlisting>
    </example>
   </section>
  </partintro>

&reference.zlib.functions;

 </reference>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

