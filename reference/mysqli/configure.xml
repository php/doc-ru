<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 082f233928bdec62c26d3ba95e8a1a2164bbed57 Maintainer: aur Status: ready -->
<!-- Reviewed: yes -->
<!-- $Revision$ -->
<section xml:id="mysqli.installation" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">

&reftitle.install;

 <para>
  Расширение <literal>mysqli</literal> было представлено с версией PHP 5.0.0.
  MySQL Native Driver был включен в PHP версии 5.3.0.
 </para>

 <section xml:id="mysqli.installation.linux">

  <title>Установка для Linux</title>

  <para>
   Большинство дистрибутивов Unix включает бинарные версии PHP, которые в дальнейшем
   могут быть установлены. Несмортря на то, что бинарные версии, как 
   правило, собраны с включенной поддержкой расширений MySQL, может
   потребоваться установка дополнительных пакетов с библиотеками расширений.
   Убедитесь, что менеджер пакетов, идущий с выбранным дистрибутивом,
   позволяет установить такие пакеты.
  </para>

  <para>
   В случае, если ваш Unix дистрибутив не поставляется с бинарным пакетом PHP
   с поддержкой расширения <literal>mysqli</literal>, то необходимо
   собрать PHP из исходного кода. Сборка PHP из исходного кода позволяет
   выделить только те расширения MySQL (а также клиентские библиотеки для каждого
   из расширений), которые требуется использовать.
  </para>

  <section>

   <title>PHP 5.0, 5.1, 5.2</title>

   <para>
    Чтобы убедиться, что расширение <literal>mysqli</literal> включено для сборки
    из исходного кода, необходимо настроить исходный код PHP
    для использования <literal>mysqli</literal>. Для этого требуется запустить скрипт
    <command>configure</command> с опцией 
    <option role="configure">--with-mysqli=mysql_config_path/mysql_config</option>,
    перед сборкой PHP. Таким образом расширение <literal>mysqli</literal> будет включено
    и для связи с MySQL Сервером будет использоваться MySQL Client Library (libmysql).
   </para>

   <para>
    В <literal>mysql_config_path</literal> содержится путь к
    программе <literal>mysql_config</literal>, которая поставляется с сервером
    MySQL.
   </para>

  </section>

  <section>
   
   <title>PHP 5.3.0+</title>

    <para>
     С версиями c PHP 5.3.0 и более поздними, для <literal>mysqli</literal>
     по умолчанию используется MySQL Native Driver. Это дает ряд преимуществ 
     в сравнении с <literal>libmysql</literal>.
    </para>

    <para>
     Использование рекомендованного MySQL Native Driver приводит к повышению 
     производительности и дает доступ к функциям, не доступным при использовании
     MySQL Client Library. По ссылке <link linkend="mysqli.overview.mysqlnd">Что
     такое MySQL Native Driver в PHP?</link> приводится обзор преимуществ MySQL
     Native Driver.
    </para>

    <para>
     Для использования MySQL Native Driver с <literal>mysqli</literal>
     необходимо изменить параметры сборки PHP: указать опцию
     <option role="configure">--with-mysqli=mysqlnd</option>.
    </para>

    <para>
     Следует иметь ввиду, что можно свободно комбинировать расширения MySQL и
     клиентские библиотеки. К примеру, возможно включить расширение MySQL
     для использования MySQL Client Library (libmysql), а расширение
     <literal>mysqli</literal> использовать с MySQL Native Driver.
     Возможны различные перестановки расширений и клиентских библиотек.
    </para>

    <para>
     Следующий пример собирает расширение MySQL для использования MySQL
     Client Library, а расширения <literal>mysqli</literal> и PDO MYSQL
     для использования MySQL Native Driver:
    </para>

<programlisting role="shell">
./configure --with-mysql=/usr/bin/mysql_config  \
--with-mysqli=mysqlnd \
--with-pdo-mysql=mysqlnd
[other options]
</programlisting>

  </section>

 </section>

 <section xml:id="mysqli.installation.windows">

  <title>Установка для Windows</title>

  <para>
   Для Windows PHP в большинстве случаев устанавливается с помощью
   установщика.
  </para>

  <section>

   <title>PHP 5.0, 5.1, 5.2</title>

   <para>
    После установки PHP необходимо произвести настройку для включения
    расширения <literal>mysqli</literal> и определения используемой им
    клиентской библиотеки.
   </para>

   <para>
    Расширение <literal>mysqli</literal> не включено по умолчанию, а
    следовательно в &php.ini; необходимо указать файл DLL 
    <filename>php_mysqli.dll</filename>. Для этого вам необходимо найти
    файл &php.ini; (обычно расположен в <filename>c:\php</filename>) и
    убедиться, что вы сняли знак комментирования (";") в начале строки
    <literal>extension=php_mysqli.dll</literal>, в разделе
    <literal>[PHP_MYSQLI]</literal>.
   </para>

   <para>
    Также, если вы хотите использовать MySQL Client Library с
    <literal>mysqli</literal>, то вам необходимо убедиться, что
    PHP может получить доступ к файлу клиентской библиотеки.
    MySQL Client Library включен в дистрибутив Windows PHP
    в виде файла <filename>libmysql.dll</filename>. Этот файл должен
    быть доступен в переменной окружения Windows <envar>PATH</envar>
    для того, чтобы его можно было успешно загрузить. По ссылке 
    "<link linkend="faq.installation.addtopath">Как мне добавить мою
    PHP директорию в Windows PATH</link>" находится статья с информацией
    о том, как это сделать. Если системная директория Windows прописана в 
    <envar>PATH</envar>, то можно скопировать <filename>libmysql.dll</filename>
    в системную директорию Windows (обычно <filename>c:\Windows\system</filename>).
    Однако такой путь не рекомендуется.
   </para>

   <para>
    При включении любого расширения PHP (например 
    <filename>php_mysqli.dll</filename>), директива PHP
    <link linkend="ini.extension-dir">extension_dir</link> должна содержать
    путь к директории, где находятся расширения PHP. Смотрите также
    <link linkend="install.windows.manual">Инструкции по Самостоятельной
     Установке для Windows</link>. Для примера в PHP 5 значением
    <literal>extension_dir</literal> является <filename>c:\php\ext</filename>.
   </para>

   <note>
    <para>
     Если при загрузке сервера появляется следующее сообщение:
     <literal>"Unable to load dynamic library './php_mysqli.dll'"</literal>,
     то система не может найти файлы <filename>php_mysqli.dll</filename> и/или
     <filename>libmysql.dll</filename>.
    </para>
   </note>
  </section>

  <section>

    <title>PHP 5.3.0+</title>

   <para>
    Для Windows, в версиях PHP 5.3 и старше, расширение <literal>mysqli</literal>
    использует MySQL Native Driver по умолчанию. Это значит, что вам не нужно
    беспокоиться о настройке доступа к файлу <filename>libmysql.dll</filename>.
   </para>

  </section>

 </section>

</section>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
