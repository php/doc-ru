<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1.5 Maintainer: tronic Status: ready -->
<!-- $Revision: 1.4 $ -->
<!-- splitted from ./en/functions/dir.xml, last change in rev 1.2 -->
  <refentry xml:id="function.readdir" xmlns="http://docbook.org/ns/docbook">
   <refnamediv>
    <refname>readdir</refname>
    <refpurpose>Получить элемент каталога по его дескриптору</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Описание</title>
     <methodsynopsis>
      <type>string</type><methodname>readdir</methodname>
      <methodparam><type>resource</type><parameter>dir_handle</parameter></methodparam>
     </methodsynopsis>
    <para>
     Возвращает имя следующего по порядку элемента каталога. Имена
     элементов возвращаются в порядке, зависящем от файловой
     системы.
    </para>
    <para>
     Обратите внимание на способ проверки значения, возвращаемого
     функцией <function>readdir</function> в приведенном ниже
     примере. В этом примере осуществляется проверка значения
     на идентичность (выражения идентичны, когда они равны и являются
     значениями одного типа - за более подробной информацией
     обратитесь к главе <link linkend="language.operators.comparison">
     Операторы сравнения</link>) значению &false;, поскольку
     в ином случае, любой элемент каталога, чье имя может быть
     выражено как &false;, остановит цикл (например, элемент с
     именем "0").
    </para>
    <para>
     <example>
      <title>Вывести список всех файлов в каталоге</title>
      <programlisting role="php">
<![CDATA[
<?php
// Обратите внимание, что оператор !== не существовал до версии 4.0.0-RC2

if ($handle = opendir('/path/to/files')) {
    echo "Дескриптор каталога: $handle\n";
    echo "Файлы:\n";

    /* Именно этот способ чтения элементов каталога является правильным. */
    while (false !== ($file = readdir($handle))) { 
        echo "$file\n";
    }

    /* Этот способ НЕВЕРЕН. */
    while ($file = readdir($handle)) { 
        echo "$file\n";
    }

    closedir($handle); 
}
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     Обратите внимание, что функция <function>readdir</function> также возвращает
     элементы с именами <literal>.</literal> и <literal>..</literal>.
     Если вы не хотите получать эти значения, просто отбрасывайте их:
     <example>
      <title>
       Получить список файлов в текущем каталоге и отбросить элементы
       с именами <literal>.</literal> и <literal>..</literal>
      </title>
      <programlisting role="php">
<![CDATA[
<?php 
if ($handle = opendir('.')) {
    while (false !== ($file = readdir($handle))) { 
        if ($file != "." && $file != "..") { 
            echo "$file\n"; 
        } 
    }
    closedir($handle); 
}
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     См.также описания функций <function>is_dir</function> и
     <function>glob</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
