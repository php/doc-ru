<?xml version="1.0" encoding="windows-1251"?>
<!-- $Revision: 1.2 $ -->
  <refentry id="function.oci-fetch-all">
   <refnamediv>
    <refname>oci_fetch_all</refname>
    <refpurpose>Выбирает все строки из результата запроса в массив</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Описание</title>
     <methodsynopsis>
      <type>int</type><methodname>oci_fetch_all</methodname>
      <methodparam><type>resource</type><parameter>statement</parameter></methodparam>
      <methodparam><type>array</type><parameter>&amp;output</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>skip</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>maxrows</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>flags</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>oci_fetch_all</function> выбирает все строки из результата
     запроса в указанный пользователем массив.
     <function>oci_fetch_all</function> возвращает количество выбранных строк
     или &false; в случае ошибки. Параметр <parameter>skip</parameter>
     указывает количество строк с начала резульата, которые следует пропустить
     (по умолчанию этот параметр равен 0, т.е. обработка начинается с самого
     начала). Параметр <parameter>maxrows</parameter> - это количество строк,
     которое требуется прочесть, начиная со строки, указанной в параметре\
     <parameter>skip</parameter> (по умолчанию равно -1, т.е. все строки).
    </para>
    <para>
     Аргумент <parameter>flags</parameter> может быть комбинацией следующих
     флагов: 
     <simplelist>
      <member>
       <constant>OCI_FETCHSTATEMENT_BY_ROW</constant>
      </member>
      <member>
       <constant>OCI_FETCHSTATEMENT_BY_COLUMN</constant> (значение по
       умолчанию)
      </member>
      <member>
       <constant>OCI_NUM</constant>
      </member>
      <member>
       <constant>OCI_ASSOC</constant>
      </member>
     </simplelist>
    </para>
    <para>
     <example>
      <title>Пример использования <function>oci_fetch_all</function></title>
      <programlisting role="php">
<![CDATA[
<?php
/* oci_fetch_all example mbritton at verinet dot com (990624) */

$conn = oci_connect("scott", "tiger");

$stmt = oci_parse($conn, "select * from emp");

oci_execute($stmt);

$nrows = oci_fetch_all($stmt, $results);
if ($nrows > 0) {
   echo "<table border=\"1\">\n";
   echo "<tr>\n";
   while (list($key, $val) = each($results)) {
      echo "<th>$key</th>\n";
   }
   echo "</tr>\n";
   
   for ($i = 0; $i < $nrows; $i++) {
      reset($results);
      echo "<tr>\n";
      while ($column = each($results)) {   
         $data = $column['value'];
         echo "<td>$data[$i]</td>\n";
      }
      echo "</tr>\n";
   }
   echo "</table>\n";
} else {
   echo "No data found<br />\n";
}      
echo "$nrows Records Selected<br />\n";
 
oci_free_statement($stmt);
oci_close($conn);
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <function>oci_fetch_all</function> возвращает &false; в случае ошибки.
    </para>
    <note>
     <para>
      В версиях PHP ниже 5.0.0 эта функция называлась
      <function>ocifetchstatement</function>. 
      В PHP 5.0.0 и выше <function>ocifetchstatement</function> является
      алиасом <function>oci_fetch_all</function>, поэтому вы можете
      продолжать использовать это имя, однако это не рекомендуется.
     </para>
    </note>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
