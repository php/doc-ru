<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
  <refentry xml:id="function.oci-error" xmlns="http://docbook.org/ns/docbook">
   <refnamediv>
    <refname>oci_error</refname>
    <refpurpose>Возвращает последнюю ошибку</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Описание</title>
     <methodsynopsis>
      <type>array</type><methodname>oci_error</methodname>
      <methodparam choice="opt"><type>resource</type><parameter>source</parameter></methodparam>
     </methodsynopsis>
    <para>
     Для большинства ошибок параметром <parameter>source</parameter> является
     соответствующий идентификатор соединения или выражения. Для ошибок во время 
     выполнения функций <function>oci_connect</function>, 
     <function>oci_new_connect</function> и <function>oci_pconnect</function> 
     этот параметр указывать не следует. <function>oci_error</function> возвращает 
     последнюю ошибку, которая была обнаружена в указанном ресурсе. В случае, 
     если ошибок не было найдено, <function>oci_error</function> возвращает &false;.
    </para>
    <para>
     <function>oci_error</function> возвращает ошибку в виде ассоциативного
     массива из четырех элементов. Элемент <literal>code</literal> содержит код
     ошибки Oracle; элемент <literal>message</literal> - строку с текстом
     ошибки; <literal>sqltext</literal> - строка, содержащая выражение SQL,
     которое вызвало ошибку, а элемент <literal>offset</literal> - указатель 
     на место в выражении, которое вызвало ошибку.
    </para>
    <note>
     <simpara>
      Элементы массива <literal>offset</literal> и <literal>sqltext</literal> 
      были добавлены начиная с версии PHP 4.3.
     </simpara>
    </note>
    <para>
     <example>
      <title>Вывод сообщения при ошибке соединения</title>
      <programlisting role="php">
<![CDATA[
$conn = @oci_connect("scott", "tiger", "mydb");
if (!$conn) {
  $e = oci_error();   // For oci_connect errors pass no handle
  echo htmlentities($e['message']);
}
]]>
      </programlisting>
     </example> 
    </para>
    <para>
     <example>
      <title>Вывод сообщения при ошибке парсинга выражения</title>
      <programlisting role="php">
<![CDATA[
$stmt = @oci_parse($conn, "select ' from dual");  // note mismatched quote
if (!$stmt) {
  $e = oci_parse($conn);  // For oci_parse errors pass the connection handle
  echo htmlentities($e['message']);
}
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title>Вывод сообщения об ошибке и SQL-выражения, в котором она возникла</title>
      <programlisting role="php">
<![CDATA[
$r = oci_execute($stmt);
if (!$r) {
  $e = oci_error($stmt); // For oci_execute errors pass the statementhandle
  echo htmlentities($e['message']);
  echo "<pre>";
  echo htmlentities($e['sqltext']);
  printf("\n%".($e['offset']+1)."s", "^");
  echo "</pre>";
}     
]]>
      </programlisting>
     </example>
    </para>
    <note>
     <para>
      В версиях PHP ниже 5.0.0 эта функция называлась <function>ocierror</function>. 
      В PHP 5.0.0 и выше <function>ocierror</function> является
      алиасом <function>oci_error</function>, поэтому вы можете
      продолжать использовать это имя, однако это не рекомендуется.
     </para>
    </note>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
