<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: e2f2172bf114599926878017ab2dce356956fa9e Maintainer: lex Status: ready -->
<!-- Reviewed: no -->
<refentry xml:id="yaf-route-regex.construct" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>Yaf_Route_Regex::__construct</refname>
  <refpurpose>Инициализирует новый экземпляр класса Yaf_Route_Regex</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <constructorsynopsis>
   <modifier>public</modifier> <methodname>Yaf_Route_Regex::__construct</methodname>
   <methodparam><type>string</type><parameter>match</parameter></methodparam>
   <methodparam><type>array</type><parameter>route</parameter></methodparam>
   <methodparam choice="opt"><type>array</type><parameter>map</parameter></methodparam>
   <methodparam choice="opt"><type>array</type><parameter>verify</parameter></methodparam>
   <methodparam choice="opt"><type>string</type><parameter>reverse</parameter></methodparam>
  </constructorsynopsis>
  <para>
  </para>

 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <variablelist>
   <varlistentry>
    <term><parameter>match</parameter></term>
    <listitem>
     <para>
      Регулярное выражение для проверки соответствия URI запроса шаблону.
      При несовпадении URI объект <classname>Yaf_Route_Regex</classname> вернёт значение &false;.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><parameter>route</parameter></term>
    <listitem>
     <para>
      Массив с правилами построения маршрута. При совпадении URI запроса с шаблоном
      объект <classname>Yaf_Route_Regex</classname> построит маршрут
      и направит запрос в модуль, контроллер и (или) действие.
     </para>
     <para>
      Ни один компонент маршрута не относится к обязательным, маршрутизатор подставит значение по умолчанию,
      если в массиве компоненту не присвоили конкретное значение.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><parameter>map</parameter></term>
    <listitem>
     <para>
      Массив для назначения имён совпадениям, которые захватил шаблон в результате сопоставления.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><parameter>verify</parameter></term>
    <listitem>
     <para>

     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><parameter>reverse</parameter></term>
    <listitem>
     <para>
      Строка для обратной сборки URL-адреса
      методом <methodname>Yaf_Route_Regex::assemble</methodname>.
      <note>
       <para>
       Параметр доступен с Yaf 2.3.0.
       </para>
      </note>
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>

  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <example>
   <title>Пример работы с классом <classname>Yaf_Route_Regex</classname></title>
   <programlisting role="php">
<![CDATA[
<?php

/**
 * Добавить в стек маршрутизатора Yaf_RouterПример маршрут на основе регулярного выражения
 */
Yaf_Dispatcher::getInstance()->getRouter()->addRoute(
    "name",
    new Yaf_Route_Regex(
        "#^/product/([^/]+)/([^/])+#", // Соответствует URI запроса, который начинается с "/product"
        array(
            'controller' => "product", // Маршрут к контроллеру продукта
        ),
        array(
            1 => "name",   // Вызов $request->getParam("name") теперь вернёт первое совпадение, которое захватил шаблон
            2 => "id",
        )
    )
);
]]>
   </programlisting>
  </example>
  <example>
   <title>Пример работы с классом <classname>Yaf_Route_Regex</classname> с версии 2.3.0</title>
   <programlisting role="php">
<![CDATA[
<?php

/**
 * Обработать результат совпадения как имя MCA
 */
Yaf_Dispatcher::getInstance()->getRouter()->addRoute(
    "name",
    new Yaf_Route_Regex(
        "#^/product/([^/]+)/([^/])+#i", // Соответствует URI запроса, который начинается с "/product"
        array(
            'controller' => ":name", // Маршрут к контроллеру, название которого получил заменитель :name
                                     // в результате захвата совпадения $1
        ),
        array(
            1 => "name",   // Вызов $request->getParam("name") вернёт первое захваченное шаблоном совпадение
            2 => "id",
        )
    )
);

]]>
   </programlisting>
  </example>
  <example>
   <title>Пример работы с классом <classname>Yaf_Route_Regex</classname> с версии 2.3.0</title>
   <programlisting role="php">
<![CDATA[
<?php

/**
 * Обработать результат совпадения как имя MCA
 */
Yaf_Dispatcher::getInstance()->getRouter()->addRoute(
    "name",
    new Yaf_Route_Regex(
        "#^/product/(?<name>[^/]+)/([^/])+#i", // Соответствует URI запроса, который начинается с "/product"
        array(
            'controller' => ":name", // Маршрут к контроллеру, название которого получил заменитель :name
                                     // в результате захвата совпадения $1
        ),
        array(
            2 => "id",
        )
    )
);
]]>
   </programlisting>
  </example>
  <example>
   <title>Пример работы с классом <classname>Yaf_Route_Regex</classname></title>
   <programlisting role="php">
<![CDATA[
<?php

/**
 * Добавить в стек маршрутизатора Yaf_Router маршрут на основе регулярного выражения
 * путём вызова метода addConfig
 */
$config = array(
    "name" => array(
        "type"  => "regex",  // Маршрут Yaf_Route_Regex
        "match" => "#(.*)#", // Совпадение с произвольным URI запроса
        "route" => array(
            'controller' => "product",  // Маршрут к контроллеру продукта
            'action'     => "dummy",    // Маршрут к бессмысленному действию
        ),
        "map" => array(
            1 => "uri",   // Вызов $request->getParam("uri") теперь вернёт URI запроса
        ),
    ),
);

Yaf_Dispatcher::getInstance()->getRouter()->addConfig(
    new Yaf_Config_Simple($config)
);
]]>
   </programlisting>
  </example>
 </refsect1>

 <refsect1 role="seealso">
  &reftitle.seealso;
  <simplelist>
   <member><methodname>Yaf_Router::addRoute</methodname></member>
   <member><methodname>Yaf_Router::addConfig</methodname></member>
   <member><classname>Yaf_Route_Static</classname></member>
   <member><classname>Yaf_Route_Supervar</classname></member>
   <member><classname>Yaf_Route_Simple</classname></member>
   <member><classname>Yaf_Route_Rewrite</classname></member>
   <member><classname>Yaf_Route_Map</classname></member>
  </simplelist>
 </refsect1>

</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
