<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1.8 Maintainer: freespace Status: ready -->
<!-- $Revision: 1.3 $ -->
<!-- splitted from ./en/functions/filesystem.xml, last change in rev 1.2 -->
  <refentry id="function.tempnam">
   <refnamediv>
    <refname>tempnam</refname>
    <refpurpose>Создаёт файл с уникальным именем</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Описание</title>
     <methodsynopsis>
      <type>string</type><methodname>tempnam</methodname>
      <methodparam><type>string</type><parameter>dir</parameter></methodparam>
      <methodparam><type>string</type><parameter>prefix</parameter></methodparam>
     </methodsynopsis>
    <para>
     Создаёт файл с уникальным именем в определённой директории.
     Если эта директория не существует, <function>tempnam</function>
     попытается создать файл во временной директории системы и
     вернуть его имя. 
    </para>
    <para>
     В версиях PHP ниже 4.0.6, поведение функции
     <function>tempnam</function> было платформозависимым.
     В Windows переменная окружения TMP была приоритетнее аргумента
     <parameter>dir</parameter>, в Linux приоритетнее была переменная
     окружения TMPDIR, а SVR4 всегда использовал ваш аргумент
     <parameter>dir</parameter>, если указанная директория существовала.
     Обратитесь к вашей документации по функции tempnam(3), если у вас
     возникнут сомнения.
    </para>
    <note>
     <simpara>
      Если PHP не может создать файл в указанной директории
      <parameter>dir</parameter>, он возвращается к директории
      по умолчанию вашей системы.
     </simpara>
    </note>
    <para>
     Возвращает имя нового временного файла или &false; в
     случае неудачи.
     <example>
      <title>Пример использования функции <function>tempnam</function></title>
      <programlisting role="php">
<![CDATA[
<?php
$tmpfname = tempnam("/tmp", "FOO");

$handle = fopen($tmpfname, "w");
fwrite($handle, "записываем в во временный файх");
fclose($handle);

// здесь мы чего-нибудь делаем

unlink($tmpfname);
?>
]]>
      </programlisting>
     </example>
    </para>
    <note>
     <simpara>
      Поведение функции изменилось начиная с версии PHP 4.0.3. Временный файл
      также создаётся чтобы избежать состояния гонки, когда файл может появится
      в файловой системе между моментом генерации строки и моментом, когда
      скрипт начнёт его создавать. Обратите внимание, что вы должны удалить
      файл вручную, если он больше вам не нужен; этот процесс не автоматизирован.
     </simpara>
    </note>
    <para>
     См. также описание функий <function>tmpfile</function> и <function>unlink</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
