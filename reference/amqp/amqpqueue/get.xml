<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: dc1dcfb27b0a3dcbaa771035ce891c3ce31a03c7 Maintainer: mch Status: ready -->
<!-- Reviewed: no -->
<!-- $Revision$ -->

<refentry xml:id="amqpqueue.get" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>AMQPQueue::get</refname>
  <refpurpose>Получить следующее сообщение из очереди</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <modifier>public</modifier> <type>mixed</type><methodname>AMQPQueue::get</methodname>
      <methodparam choice="opt"><type>int</type><parameter>flags</parameter></methodparam>
     </methodsynopsis>
     <para>
    Получает следующее сообщение из очереди. Если больше нет сообщений в очереди, то функция 
    немедленно возвращает &false;. Функция является неблокирующим аналогом метода <methodname>AMQPQueue::consume</methodname>.
     </para>
     <para>
      В данный момент в качестве параметра <parameter>flags</parameter> поддерживается только флаг <constant>AMQP_AUTOACK</constant>. Если этот флаг указан, тогда возвращаемое сообщение будет автоматически помечено брокером как подтвержденное в тот момент, когда фреймы отправлены клиенту.
     </para>
    </refsect1>

    <refsect1 role="parameters">
     &reftitle.parameters;
     <para>
      <variablelist>
      <varlistentry>
       <term><parameter>flags</parameter></term>
       <listitem>
        <para>
         Битовая маска, состоящая из поддерживаемых флагов для вызываемого метода. В данный момент поддерживается только флаг <constant>AMQP_AUTOACK</constant>. Если это значение не указано, будет использоваться значение из <literal>amqp.auto_ack</literal>.
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
     </para>
    </refsect1>

    <refsect1 role="returnvalues">
     &reftitle.returnvalues;
     <para>
      Экземпляр класса <classname>AMQPEnvelope</classname>, представляющий взятое из очереди сообщение, или &false;.
     </para>
    </refsect1>

    <refsect1 role="examples">
     &reftitle.examples;
     <para>
      <example>
       <title>Пример использования <methodname>AMQPQueue::get</methodname></title>
       <programlisting role="php">
   <![CDATA[
   <?php

   /* создание соединения, используя все учетные данные по умолчанию: */
   $connection = new AMQPConnection();
   $connection->connect();

   $channel = new AMQPChannel($connection);

   /* создание объекта очереди */
   $queue = new AMQPQueue($channel);

   //объявление очереди
   $queue->declare('myqueue');

   //получение сообщений
   $messages = $queue->get(AMQP_AUTOACK);

   echo $message->getBody();

   ?>
   ]]>
       </programlisting>
      </example>
     </para>
    </refsect1>

   </refentry>

   <!-- Keep this comment at the end of the file
   Local variables:
   mode: sgml
   sgml-omittag:t
   sgml-shorttag:t
   sgml-minimize-attributes:nil
   sgml-always-quote-attributes:t
   sgml-indent-step:1
   sgml-indent-data:t
   indent-tabs-mode:nil
   sgml-parent-document:nil
   sgml-default-dtd-file:"~/.phpdoc/manual.ced"
   sgml-exposed-tags:nil
   sgml-local-catalogs:nil
   sgml-local-ecat-files:nil
   End:
   vim600: syn=xml fen fdm=syntax fdl=2 si
   vim: et tw=78 syn=sgml
   vi: ts=1 sw=1
   -->
