<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: ac04b2ea214243eaef651b87216b9841d13cd492 Maintainer: aur Status: ready -->
<!-- Reviewed: yes -->
<!-- $Revision$ -->
<!-- Membership: core -->

<book xml:id="book.session" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Управление сессиями</title>
 <titleabbrev>Сессии</titleabbrev>

 <!-- {{{ preface -->
 <preface xml:id="intro.session">
  &reftitle.intro;
  <para>
   Поддержка сессий в PHP заключается в способе сохранения некоторых данных 
   между несколькими последовательными доступами. Это позволяет наилучшим образом 
   разработать индивдуальные приложения и увеличить привлекательность 
   веб-сайта.
  </para>
  <para>
   Каждому посетителю сайта присваивается уникальный идентификатор, 
   называемый идентификатором сессии (session id).
   Он хранится либо в cookie на стороне пользователя, либо передается через URL.
  </para>
  <para>
   Поддержка сессий позволяет регистрировать произвольное количество переменных 
   с целью их сохранения между запросами. В тот момент, когда посетитель 
   получает доступ к сайту, PHP проверяет (автоматически, если <link
   linkend="ini.session.auto-start">session.auto_start</link> установлено в 1,
   или по запросу явным образом через вызов <function>session_start</function> 
   или неявным через <function>session_register</function>), если определенный 
   идентификатор сессии послан вместе с запросом. Если это так, 
   восстанавливается сохраненное ранее окружение.
  </para>
  <caution>
   <para>
    При включенном <link linkend="ini.session.auto-start">
    session.auto_start</link>, единственным способом помещения объектов в 
    сессию останется загрузка описаний класса с помощью  
    <link linkend="ini.auto-prepend-file">auto_prepend_file</link>. 
    В противном случае придется выполнить сериализацию 
    (<function>serialize</function>) и десериализацию
    (<function>unserialize</function>) объекта вручную.
   </para>
  </caution>
  <para>
   Все зарегистрированные переменные сериализуются после выполнения запроса. 
   Зарегистрированные, но неопределенные переменные помечаются как 
   неопределенные. При последующих доступах они не определяются сессионным 
   модулем пока пользователь заново не определит их.
  </para>
  <warning>
   <para>
    Некоторые типы данных не могут быть сериализованы и, таким образом, 
    не могут храниться в сессии. Эти типы включают в себя переменные типа
    <type>resource</type> и объекты 
    с циклическими ссылками (например, объекты, которые передают ссылку сами 
    на себя через другой объект).
   </para>
  </warning>
  <note>
   <para>
    При работе с сессиями обратите внимание, что запись в сессии не создается 
    прежде чем переменная регистрируется через функцию 
    <function>session_register</function> или через добавление нового ключа 
    в суперглобальном массиве <varname>$_SESSION</varname>. Это правило 
    действует вне зависимости от того, была ли сессия запущена 
    с использованием функции <function>session_start</function>. 
   </para>
  </note>
  <note>
   <para>
    В PHP 5.2.2 была добавлена незадокументированная возможность, 
    позволяющая хранить сессионный файлы в каталоге "/tmp" даже 
    если <link linkend="ini.open-basedir">open_basedir</link> 
    была включена и "/tmp" не был явным образом добавлен в список 
    разрешенных путей. Эта особенность была убрана из PHP 
    по состоянию на PHP 5.3.0.
   </para>
  </note>
 </preface>
 <!-- }}} -->

 &reference.session.setup;
 &reference.session.constants;
 &reference.session.examples;
 &reference.session.security;
 &reference.session.reference;

</book>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

