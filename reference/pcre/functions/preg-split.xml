<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision: 1.3 $ -->
<!-- EN-Revision: 1.10 Maintainer: young Status: ready -->
<!-- splitted from ./en/functions/pcre.xml, last change in rev 1.2 -->
  <refentry id="function.preg-split">
   <refnamediv>
    <refname>preg_split</refname>
    <refpurpose>Разбивает строку по регулярному выражению</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Описание</title>
     <methodsynopsis>
      <type>array</type><methodname>preg_split</methodname>
      <methodparam><type>string</type><parameter>pattern</parameter></methodparam>
      <methodparam><type>string</type><parameter>subject</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>limit</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>flags</parameter></methodparam>
     </methodsynopsis>

    <para>
     Возвращает массив, состоящий из подстрок заданной строки 
     <parameter>subject</parameter>,  которая разбита по границам, соответствующим шаблону
     <parameter>pattern</parameter>.
    </para>

    <para>
     В случае, если параметр <parameter>limit</parameter> указан, функция возвращает
     не более, чем <parameter>limit</parameter> подстрок. Специальное значение
     <parameter>limit</parameter>, равное  -1, подразумевает отсутствие ограничения,
	 это весьма полезно для указания еще одного опционального параметра
     <parameter>flags</parameter>.
    </para>

    <para>
     <parameter>flags</parameter> может быть произвольной комбинацией следующих флагов
	 (соединение происходит при помощи оператора '|'):
      <variablelist>
       <varlistentry>
        <term><constant>PREG_SPLIT_NO_EMPTY</constant></term>
        <listitem>
         <simpara>
		В случае, если этот флаг указан, функция <function>preg_split</function>
		вернет только непустые подстроки.
         </simpara>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><constant>PREG_SPLIT_DELIM_CAPTURE</constant></term>
        <listitem>
         <simpara>
		 В случае, если этот флаг указан, выражение, заключенное в круглые скобки в 
		 разделяющем шаблоне, также извлекается из заданной строки и возвращается
		 функцией. Этот флаг был добавлен в PHP 4.0.5.
         </simpara>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><constant>PREG_SPLIT_OFFSET_CAPTURE</constant></term>
        <listitem>
         <para>
		  В случае, если этот флаг указан, для каждой найденной подстроки, будет указана
		  ее позиция в исходной строке. Необходимо помнить, что этот флаг меняет 
		  формат возвращаемых данных: каждое вхождение возвращается в виде массива,
		  в нулевом элементе которого содержится найденная подстрока, а в первом - смещение.
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
    </para>
     <para>
     <example>
      <title><function>preg_split</function> пример: Получение подстрок из заданного текста</title>
      <programlisting role="php">
<![CDATA[
<?php
// разбиваем строку по произвольному числу запятых и пробельных символов, 
// которые включают в себя  " ", \r, \t, \n и \f
$keywords = preg_split("/[\s,]+/", "hypertext language, programming");
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title>Разбиваем строку на составляющие символы</title>
      <programlisting role="php">
<![CDATA[
<?php
$str = 'string';
$chars = preg_split('//', $str, -1, PREG_SPLIT_NO_EMPTY);
print_r($chars);
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title>Разбиваем строку с указанием смещения для каждой из найденных подстрок</title>
      <programlisting role="php">
<![CDATA[
<?php
$str = 'hypertext language programming';
$chars = preg_split('/ /', $str, -1, PREG_SPLIT_OFFSET_CAPTURE);
print_r($chars);
?>
]]>
      </programlisting>
      <para>
      На выходе получаем:
      </para>
      <screen>
<![CDATA[
Array
(
    [0] => Array
        (
            [0] => hypertext
            [1] => 0
        )

    [1] => Array
        (
            [0] => language
            [1] => 10
        )

    [2] => Array
        (
            [0] => programming
            [1] => 19
        )

)
]]>
      </screen>
     </example>
    </para>
    <note>
     <para>
      Параметр <parameter>flags</parameter> был добавлен в PHP 4 Beta 3.
     </para>
    </note>
    <para>
     Смотрите также <function>spliti</function>, <function>split</function>,
     <function>implode</function>, <function>preg_match</function>,
     <function>preg_match_all</function>, и
     <function>preg_replace</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
